{{- if .Values.apps.plexUtills.enabled -}}

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plex-utills
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "15"
spec:
  project: core
  source:
    repoURL: https://charts.truecharts.org
    targetRevision: 2.0.2
    chart: plex-utills
    helm:
      values: |
        {{- .Values.apps.plexUtills.valuesOverrides | toYaml | nindent 8 }}

  destination:
    server: https://kubernetes.default.svc
    namespace: plex-utills

  # Sync policy
  syncPolicy:
    automated: 
      prune: true
      selfHeal: true 
      allowEmpty: false
    syncOptions:
    - CreateNamespace=true
    - PrunePropagationPolicy=foreground
    - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 30s
  revisionHistoryLimit: 3

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plex-utills-ingress
  namespace: plex-utills
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/whitelist-source-range: 86.1.85.130/32
spec:
  tls:
  - hosts:
    - plex-utills.k8s.domingo.link
    secretName: plex-utills-ingress-tls
  rules:
    - host: plex-utills.k8s.domingo.link
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: plex-utills
                port:
                  number: 10380


{{- end -}}
